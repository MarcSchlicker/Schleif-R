using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Originteleportation : MonoBehaviour
{
    //Drehen um die Drehung die der Schleifer falsch ist ist und dann dann den Vektor berechnen,
    //der zwischen aktuelle und gewünschte Position des Schleifers und um diesen den Rig
     
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
